import 'package:flutter/material.dart';

import 'widget.dart';

class MoodInteractionView extends StatelessWidget {
  const MoodInteractionView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: MoodInteractionDemo());
  }
}
